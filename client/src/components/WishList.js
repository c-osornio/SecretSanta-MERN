import React from 'react'

const WishList = ({party, member, setMember, id, memberId}) => {
    return (
        <>
            Wishlist belongs to {member.name}
        </>
    )
}

export default WishList